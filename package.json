{
  "name": "data-factory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": { "phaser": "^4.2.1" },
  "devDependencies": { "typescript": "^5.9.3", "vite": "^7.1.0", "vitest": "^3.2.4", "@playwright/test": "^1.55.0" }
}
